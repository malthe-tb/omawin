# Roadmap and fuzzy ideas for the project

## Roadmap

* Build TUI tool for invoking scripts and executing regular workflows via the terminal
* Implement full system theme and styling, porting from omarchy 
* Omawin app for using the system (similar to TUI but GUI based)
* WSL setup
* Neovim setup 

## Fuzzy ideas

* Neovim x VSCode setup investigation
* Include special purpose tool packages  (e.g. softwares for embedded systems development)

## Improvements

* Better top bar that can follow system style and themes  (komorebi built in topbar?)
* Better (neo)vim setup for vscode
* Better terminal alternatives? 
* Link configs to the their actual locations (.config) and not inside this repo (so that if you change something in vscode or similar you don't have to manually run the fetch configs script)
* vim keymaps for navigating windows and using the terminal? 